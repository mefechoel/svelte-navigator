import { NavigatorHistory } from "@svelte-navigator/history";
import { LocalAction } from "../SvelteInternal";

export declare const link: LocalAction<{ history: NavigatorHistory }>;

export declare const links: LocalAction<{ history: NavigatorHistory }>;
